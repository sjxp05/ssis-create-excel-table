{
  "meta": {
    "document": "장애인활동지원 급여비용 등에 관한 고시 (제2025-226호)",
    "effective_date": "2026-01-01",
    "currency": "KRW",
    "rounddown_digit_convention": { "-1": "10원 미만 절사", "-2": "100원 미만 절사" }
  },

  "rules": [
    {
      "field": "활동보조_30분",
      "source": "고시",
      "params": ["활동보조_기본단가"],
      "steps": [
        { "op": "multiply", "inputs": ["활동보조_기본단가", 0.5], "output": "temp1" },
        { "op": "rounddown", "input": "temp1", "digit": -1, "output": "활동보조_30분" }
      ]
    },
    {
      "field": "활동보조_30분_심야",
      "source": "고시",
      "params": ["활동보조_30분_단가"],
      "steps": [
        { "op": "multiply", "inputs": ["활동보조_30분_단가", 1.5], "output": "temp1" },
        { "op": "rounddown", "input": "temp1", "digit": -1, "output": "활동보조_30분_심야" }
      ]
    },
    {
      "field": "방문목욕_40분",
      "source": "고시",
      "params": ["방문목욕_기본단가"],
      "steps": [
        { "op": "multiply", "inputs": ["방문목욕_기본단가", 0.8], "output": "temp1" },
        { "op": "rounddown", "input": "temp1", "digit": -1, "output": "방문목욕_40분" }
      ]
    },

    {
      "field": "본인부담금",
      "source": "협조문",
      "params": ["월한도액", "본인부담률", "상한액"],
      "steps": [
        { "op": "multiply", "inputs": ["월한도액", "본인부담률"], "output": "temp1" },
        { "op": "rounddown", "input": "temp1", "digit": -2, "output": "temp2" },
        { "op": "cap_max", "input": "temp2", "max": "상한액", "output": "본인부담금" }
      ]
    },

    {
      "field": "등급코드_규칙",
      "source": "하드코딩",
      "params": [],
      "steps": []
    },
    {
      "field": "산정특례_번호_규칙",
      "source": "하드코딩",
      "params": [],
      "steps": []
    },
    {
      "field": "정부지원금",
      "source": "하드코딩",
      "params": ["월한도액", "본인부담금"],
      "steps": [
        { "op": "subtract", "inputs": ["월한도액", "본인부담금"], "output": "정부지원금" }
      ]
    },
    {
      "field": "산정특례_월한도액",
      "source": "하드코딩",
      "params": ["기본월한도액", "추가특성_월한도액", "학교여부", "직장여부"],
      "steps": [
        { "op": "sum", "inputs": ["기본월한도액", "추가특성_월한도액"], "output": "temp1" },
        { "op": "add_if", "condition": "학교여부", "inputs": ["temp1", "추가특성_월한도액[학교]"], "output": "temp2" },
        { "op": "add_if", "condition": "직장여부", "inputs": ["temp2", "추가특성_월한도액[직장]"], "output": "산정특례_월한도액" }
      ]
    },
    {
      "field": "확장형_월한도액",
      "source": "하드코딩",
      "params": ["기본형_월한도액", "기본단가"],
      "steps": [
        { "op": "multiply", "inputs": [22, "기본단가"], "output": "temp1" },
        { "op": "subtract", "inputs": ["기본형_월한도액", "temp1"], "output": "temp2" },
        { "op": "roundup", "input": "temp2", "digit": -3, "unit": "1,000원 단위 올림", "output": "확장형_월한도액" }
      ]
    }
  ]
}